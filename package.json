{
  "name": "config-compliance-dashboard",
  "version": "0.1.0",
  "description": "Roll up AWS Config compliance data across accounts into a single static dashboard",
  "license": "MIT",
  "author": "Brian Dixon",
  "type": "module",
  "scripts": {
    "collect": "tsx src/index.ts collect",
    "build": "tsx src/index.ts build",
    "lint": "eslint src --ext .ts,.tsx",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-config-service": "^3.600.0",
    "@aws-sdk/client-sts": "^3.600.0",
    "chart.js": "^4.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "eslint": "^9.5.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0",
    "vitest": "^1.6.0"
  }
}
